# คู่มือเติมข้อมูลระบบฝึกอบรม

ไฟล์หลักสำหรับเติมข้อมูลคือ `data.js`

## โครงสร้างที่ต้องใช้

- `categoryOrder` คือ ลำดับหมวดที่จะแสดงหน้าแรก
- `videoUrls` คือ ลิงก์ YouTube ของแต่ละหัวข้อ ใส่ตามเลข `id`
- `topicMedia` คือ รูปขั้นตอนและรูป Assembly/รูปที่เกี่ยวข้อง ใส่ตามเลข `id`
- `topics` คือ ข้อมูลหัวข้อทั้งหมด

## เพิ่มหัวข้อใหม่

เพิ่ม object ใหม่ใน `trainingData.topics` โดยใช้รูปแบบนี้:

```js
{
  id: 11,
  icon: "",
  title: "ชื่อหัวข้อ",
  severity: "medium",
  category: "ชื่อหมวด",
  issuesList: [
    {
      issue: "อาการเสีย / สิ่งที่พบ",
      solution: "วิธีแนะนำหรือวิธีแก้ไข",
      talkingPoints: [
        "จุดพูดกับลูกค้าข้อ 1",
        "จุดพูดกับลูกค้าข้อ 2"
      ],
      fix: "ผลกระทบถ้าไม่แก้ไข"
    }
  ],
  steps: [
    "ขั้นตอนตรวจสอบข้อ 1",
    "ขั้นตอนตรวจสอบข้อ 2"
  ]
}
```

## ใส่รูป

วางไฟล์รูปไว้ในโฟลเดอร์เดียวกับ `index.html` หรือสร้างโฟลเดอร์ `images/` แล้วใส่ path ใน `topicMedia`

```js
11: {
  stepIndex: 0,
  title: "รูปประกอบขั้นตอนตรวจสอบ",
  items: [
    { src: "images/topic11-step1.jpg", caption: "คำอธิบายรูป" }
  ],
  related: [
    { src: "images/topic11-assembly.jpg", caption: "รูป Assembly ที่เกี่ยวข้อง" }
  ]
}
```

ถ้ายังไม่มีรูป ให้เว้น `src` ไว้ก่อน ระบบจะแสดงช่องเตรียมใส่รูปแทน
